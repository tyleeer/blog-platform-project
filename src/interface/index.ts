export type Itopic = {
  title: string;
  content: string;
  pic: string;
  writer: string;
  publish: number;
  file: string[];
};

export interface Ifooter {
  title: string;
  list: string[];
}
